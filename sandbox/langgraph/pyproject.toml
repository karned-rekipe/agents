[project]
name = "rekipe-agent-langgraph"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = [
    "langchain-openai>=0.3.0",
    "langchain-mcp-adapters>=0.1.0",
    "langgraph>=0.4.0",
    "pyyaml>=6.0",
    "loguru>=0.7",
]

[tool.uv]
package = false
